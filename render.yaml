services:
  - type: web
    name: asistente-cotizaciones
    runtime: docker
    plan: free
    region: oregon
    dockerfilePath: ./Dockerfile
    dockerContext: .
    envVars:
      - key: OPENAI_API_KEY
        sync: false
      - key: WHATSAPP_API_KEY
        sync: false
